# Kernel en C — Episode 2

| Archivo | Descripción |
|---------|-------------|
| `main.c` | `main()` — punto de entrada tras la transición a long mode |
| `vga.c` | Funciones auxiliares de consola (opcional si Episode 1 ya imprime en ASM) |

Compilación prevista: `gcc -ffreestanding -fno-pic -mno-red-zone -c`.
